# iWiki API 文档发布说明

## 背景

平台内 Markdown 文档已改为跳转 iWiki。需在空间 `space-R0M8mXw2` 维护完整 API / 计费 / 模型文档（风格参考 DeepSeek API Docs）。

## 结论

- 源稿：`基础服务/api-platform/docs/iwiki/*.md`
- 发布脚本：`docs/iwiki/publish.py`（注意：`/auth/me` 不支持 `iwk_` Token，脚本改用 `/spaces` 鉴权）
- 已发布到 `space-R0M8mXw2`：
  - 首页：https://ssemarket.cn/iwiki/page/page-Ao-ohAUa
  - 空间：https://ssemarket.cn/iwiki/space/space-R0M8mXw2
  - 含子文档：首次调用、模型与价格、计费、API 列表/Completions、流式、多模态、错误码、限流、控制台指南

## 未决

无。更新文档时用同一 Token 再跑 `publish.py` 即可覆盖同名页。
